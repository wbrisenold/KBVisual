# 2026-07-14 — Visual audit and polish round

## Completed
- Unified all gold colors to `yellow-700` across Pricing.tsx and Home.tsx
- Removed `filter: blur()` from all scroll-in animations (FAQ.tsx, Pricing.tsx)
- Halved all animation durations across About.tsx, Pricing.tsx, GoogleReviews.tsx, FAQ.tsx
- Fixed low-contrast text in About.tsx (`stone-500` → `stone-600`) and Pricing.tsx (`yellow-700/80` → `yellow-700/90`)
- Added skip-to-content link + `<main>` landmark for accessibility
- Removed hero floating frames, GSAP parallax, and related code
- Fixed PhotoSwipe lightbox padding/maxZoomLevel
- Replaced blank Suspense fallback with branded skeleton
- Gallery desktop cleanup: reduced gradient, removed maximize icon, increased grid gap
- Glassmorphism nav + featured pricing cards
- Unslop copy improvements (contractions)
- Created `.github/workflows/ci.yml` with TypeScript check + build + e2e + matrix
- Created `.agent-memory/` CoALA-structured memory store

## Decisions
- Keep custom camera cursor despite `design-taste-frontend` flagging it (brand identity)
- Keep framer-motion for entry/scroll but prefer CSS for hover/tap
- Skip husky for now — npm run verify covers the same gates
- Skip motion constant extraction for now — YAGNI for current scale

## Build
- `npm run check` ✓ (0 errors)
- `npm run build` ✓ (1.5s)
- `npm run verify` — run before merge
